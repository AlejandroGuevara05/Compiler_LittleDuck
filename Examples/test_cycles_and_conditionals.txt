program proquacks;

var i, j, numero, suma, temp, esPar : int;
    mensaje : string;

main
{
    i = 1;
    suma = 0;
    mensaje = "inicio";

    # print("Ciclo externo:");

    do{
        j = 1;
        # print("i =", i);

        do{
            numero = i * j;
            temp = numero;

            do{
                temp = temp - 2;
            }
            while(temp > 1);

            if (temp == 0)
            {
                mensaje = "par";
                suma = suma + numero;
            }else{
                mensaje = "impar";
                suma = suma - numero;
            };

            # print("  j =", j, "numero =", numero, "->", mensaje, "suma =", suma);
            j = j + 1;
        }
        while(j <= 3);

        if (suma > 5)
        {
            # print("SUMA MAYOR A 5 - continuando...");
        }else{
            # print("suma pequeña - continuando...");
        };

        i = i + 1;
    }
    while(i <= 2);

    print("FIN - Suma total:", suma);
}
end